# Copy this file to config.py and fill in your values
# Make sure config.py is in your .gitignore!

API_TOKEN = "your_api_token_here"
SPACE_ID = "your_space_id_here"
STRUCTURE_ID = "your_structure_id_here"

# To use this config instead of environment variables:
# 1. Copy this file to config.py
# 2. Fill in your actual values above
# 3. In import_books.py, replace the environment variable lines with:
#    from config import API_TOKEN, SPACE_ID, STRUCTURE_ID

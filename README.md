# goodreads-capacities-importer
Py script that takes books exported from goodreads and imports them to Capacities. Only uploads books that have been read. Also doesn't actually work rn since Capacities API is in early development and doesn't allow creation of objects. 
